// lib/screens/awards_screen.dart
import 'package:flutter/material.dart';

class AwardsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Awards'),
      ),
      body: Center(
        child: Text('Awards Screen'),
      ),
    );
  }
}
